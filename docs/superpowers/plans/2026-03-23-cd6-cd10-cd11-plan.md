# CD.6 + CD.10 + CD.11 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Gorev sistemi genisletme (haftalik + 12 gunluk gorev), sentez hucre pulse/glow efekti, ve Firebase push notification entegrasyonu.

**Architecture:** 3 bagimsiz subsystem: (1) Quest data model + provider + persistence, (2) GelCellPainter glow + SynthesisPulseCell wrapper, (3) firebase_messaging + flutter_local_notifications + platform config. Her biri bagimsiz test edilebilir.

**Tech Stack:** Flutter 3.41+, Riverpod, Supabase, firebase_messaging, flutter_local_notifications, timezone

**Spec:** `docs/superpowers/specs/2026-03-23-cd6-cd10-cd11-design.md`

---

## File Map

### CD.10 — Gorev Sistemi
| Action | Path | Responsibility |
|--------|------|----------------|
| Modify | `lib/game/meta/quests.dart` | Quest `id` field + 6 yeni gorev + havuz genisletme |
| Modify | `lib/providers/quest_provider.dart` | Progress key → `quest.id`, haftalik tracking, ISO week seed |
| Modify | `lib/data/local/local_repository.dart` | Haftalik persistence metodlari |
| Modify | `lib/features/home_screen/widgets/quest_bar.dart` | Haftalik gorev gosterimi |
| Modify | `test/features/quest_overlay_test.dart` | Yeni gorev testleri |

### CD.6 — Jel Morph Efekti
| Action | Path | Responsibility |
|--------|------|----------------|
| Modify | `lib/features/game_screen/gel_cell_painter.dart` | `isGlowing` param + cache invalidation |
| Modify | `lib/features/game_screen/effects/cell_effects.dart` | `SynthesisPulseCell` widget |
| Modify | `lib/features/game_screen/game_cell_widget.dart` | Glow param + pulse wrapper |

### CD.11 — Push Notification
| Action | Path | Responsibility |
|--------|------|----------------|
| Modify | `pubspec.yaml` | Yeni bagimliliklar |
| Modify | `lib/services/notification_service.dart` | Interface genisletme + FirebaseNotificationService |
| Modify | `lib/providers/service_providers.dart` | notificationServiceProvider |
| Modify | `lib/main.dart` | Init + lifecycle observer |
| Modify | `lib/data/remote/remote_repository.dart` | saveDeviceToken |
| Modify | `lib/features/home_screen/home_screen.dart` | Scheduler entegrasyonu |
| Modify | `lib/features/game_screen/game_callbacks.dart` | Comeback notification |
| Modify | `android/app/src/main/AndroidManifest.xml` | Izinler |
| Modify | `ios/Runner/Info.plist` | UIBackgroundModes |
| Create | `ios/Runner/Runner.entitlements` | Push capability |
| Create | `supabase/migrations/20260323_device_tokens.sql` | Token tablosu |
| Modify | `test/services/notification_service_test.dart` | Yeni testler |

---

## Task 1: CD.10 — Quest `id` alani ve gunluk havuz genisletme

**Files:**
- Modify: `lib/game/meta/quests.dart`
- Modify: `test/features/quest_overlay_test.dart`

- [ ] **Step 1: Quest class'a `id` alani ekle**

`quests.dart` — `Quest` class'ina `final String id` required parametresi ekle. Mevcut `kDailyQuestPool` ve `kWeeklyQuestPool` ogelerine unique id ata (`d_cl10`, `d_ms5`, `d_rc3`, `d_dp1`, `d_pg3`, `d_rs800` ve haftalik `w_cl100`, vb.).

- [ ] **Step 2: 6 yeni gunluk gorev ekle**

`kDailyQuestPool`'a 6 yeni Quest ekle (toplam 12):
```dart
Quest(id: 'd_cl15', type: QuestType.clearLines, description: '15 satir temizle', targetCount: 15, xpReward: 40, gelReward: 3),
Quest(id: 'd_rs500', type: QuestType.reachScore, description: '500 puana ulas', targetCount: 500, xpReward: 35, gelReward: 2),
Quest(id: 'd_ms8', type: QuestType.makeSyntheses, description: '8 sentez yap', targetCount: 8, xpReward: 50, gelReward: 4),
Quest(id: 'd_rc5', type: QuestType.reachCombo, description: '5 kombo yap', targetCount: 5, xpReward: 60, gelReward: 4),
Quest(id: 'd_pg5', type: QuestType.playGames, description: '5 oyun oyna', targetCount: 5, xpReward: 45, gelReward: 3),
Quest(id: 'd_cl20', type: QuestType.clearLines, description: '20 satir temizle', targetCount: 20, xpReward: 55, gelReward: 4),
```

- [ ] **Step 3: Testleri guncelle**

`quest_overlay_test.dart` — pool size testini 6→12 guncelle. Yeni gorev id'lerinin unique oldugunu dogrula.

- [ ] **Step 4: Testleri calistir**

Run: `flutter test test/features/quest_overlay_test.dart -v`
Expected: All tests PASS

- [ ] **Step 5: Commit**

```bash
git add lib/game/meta/quests.dart test/features/quest_overlay_test.dart
git commit -m "feat(quest): add id field to Quest, expand daily pool to 12"
```

---

## Task 2: CD.10 — Progress key degisikligi ve haftalik tracking

**Files:**
- Modify: `lib/providers/quest_provider.dart`
- Modify: `lib/data/local/local_repository.dart`

- [ ] **Step 1: Progress key formatini quest.id bazli yap**

`quest_provider.dart` — `incrementQuestProgress` fonksiyonunda key formatini degistir:
```dart
// Eski: final key = '${type.name}_d';
// Yeni: quest.id bazli
```
`getQuestProgress` ve `isCompleted` metodlarini da `quest.id` bazli guncelle.

- [ ] **Step 2: Haftalik persistence metodlari ekle**

`local_repository.dart`'a ekle:
```dart
Future<Map<String, int>> getWeeklyQuestProgress() async { ... }
Future<void> saveWeeklyQuestProgress(Map<String, int> progress) async { ... }
String? getWeeklyQuestWeek() => _prefs.getString('weekly_quest_week');
Future<void> saveWeeklyQuestWeek(String week) async { ... }
```

- [ ] **Step 3: QuestProgress'e weeklyQuests ekle**

`quest_provider.dart` — `QuestProgress` class'ina `weeklyQuests: List<Quest>` alani ekle. Provider'da ISO week seed ile haftalik gorev secimi yap:
```dart
int _isoWeekNumber(DateTime date) {
  final jan4 = DateTime(date.year, 1, 4);
  final dayOfYear = date.difference(DateTime(date.year, 1, 1)).inDays;
  return ((dayOfYear - date.weekday + 10) ~/ 7);
}
```

- [ ] **Step 4: incrementQuestProgress haftalik guncelleme**

Hem gunluk hem haftalik gorevleri kontrol et — `QuestType` eslesirse `_w` prefixli key'i de guncelle.

- [ ] **Step 5: Haftalik reset mekanizmasi**

Provider init'te ISO hafta key kontrolu: hafta degistiyse `w_*` progress temizle.

- [ ] **Step 6: Testleri calistir**

Run: `flutter test test/features/quest_overlay_test.dart -v`
Expected: All tests PASS

- [ ] **Step 7: Commit**

```bash
git add lib/providers/quest_provider.dart lib/data/local/local_repository.dart
git commit -m "feat(quest): id-based progress keys, weekly tracking + persistence"
```

---

## Task 3: CD.10 — QuestBar haftalik gosterim

**Files:**
- Modify: `lib/features/home_screen/widgets/quest_bar.dart`

- [ ] **Step 1: QuestBar'da haftalik gorev satirini ekle**

Gunluk progress bar'in altina haftalik progress bar (kOrange accent). `questProvider` artik `weeklyQuests` donduruyor — bu listeyi render et.

- [ ] **Step 2: Haftalik tamamlanma sayaci**

`completed_weekly / total_weekly` formatinda.

- [ ] **Step 3: Analyze + test**

Run: `flutter analyze lib/features/home_screen/widgets/quest_bar.dart && flutter test test/features/home_screen_test.dart -v`
Expected: 0 issues, all tests PASS

- [ ] **Step 4: Commit**

```bash
git add lib/features/home_screen/widgets/quest_bar.dart
git commit -m "feat(quest): weekly quest progress display in QuestBar"
```

- [ ] **Step 5: Code review — CD.10 tamamlandi**

Code reviewer agent ile review yap. Bulgulari `_dev/tasks/todo.md` backlog'a ekle.

---

## Task 4: CD.6 — GelCellPainter isGlowing parametresi

**Files:**
- Modify: `lib/features/game_screen/gel_cell_painter.dart`

- [ ] **Step 1: `isGlowing` constructor parametresi ekle**

```dart
GelCellPainter({
  required this.color,
  required this.borderRadius,
  required Animation<double> breathAnimation,
  required this.breathPhase,
  this.isGlowing = false,  // YENi
}) ...
```

- [ ] **Step 2: Glow parametrelerini isGlowing bazli degistir**

```dart
// Dis glow
final glowBlur = isGlowing ? 12.0 : 6.0;
final glowAlpha = isGlowing ? 0.60 : 0.40;

// Specular highlight
final specAlpha = isGlowing ? 0.90 : (0.62 + 0.18 * breathMod);

// Ic parlama
final innerAlpha = isGlowing ? 0.60 : (0.35 + 0.12 * breathMod);
```

- [ ] **Step 3: Cache invalidation**

`_cachedGlowPaint` cache'ini `isGlowing` degisiminde invalidate et:
```dart
if (_cachedGlowPaint == null || _cachedGlowColor != color || _cachedGlowState != isGlowing) {
  _cachedGlowColor = color;
  _cachedGlowState = isGlowing;
  _cachedGlowPaint = Paint()
    ..color = color.withValues(alpha: glowAlpha)
    ..maskFilter = MaskFilter.blur(BlurStyle.normal, glowBlur);
}
```

- [ ] **Step 4: shouldRepaint guncelle**

```dart
@override
bool shouldRepaint(GelCellPainter old) =>
    old.color != color ||
    old.borderRadius != borderRadius ||
    old.breathPhase != breathPhase ||
    old.isGlowing != isGlowing;  // YENi
```

- [ ] **Step 5: Analyze**

Run: `flutter analyze lib/features/game_screen/gel_cell_painter.dart`
Expected: No issues

- [ ] **Step 6: Commit**

```bash
git add lib/features/game_screen/gel_cell_painter.dart
git commit -m "feat(visual): add isGlowing param to GelCellPainter with cache invalidation"
```

---

## Task 5: CD.6 — SynthesisPulseCell + GameCellWidget entegrasyonu

**Files:**
- Modify: `lib/features/game_screen/effects/cell_effects.dart`
- Modify: `lib/features/game_screen/game_cell_widget.dart`

- [ ] **Step 1: SynthesisPulseCell widget olustur**

`cell_effects.dart`'a SquashStretchCell pattern'inde yeni widget:
```dart
class SynthesisPulseCell extends StatefulWidget {
  const SynthesisPulseCell({super.key, required this.isActive, required this.child});
  final bool isActive;
  final Widget child;
  @override
  State<SynthesisPulseCell> createState() => _SynthesisPulseCellState();
}

class _SynthesisPulseCellState extends State<SynthesisPulseCell>
    with SingleTickerProviderStateMixin {
  AnimationController? _ctrl;

  AnimationController _ensureController() {
    return _ctrl ??= AnimationController(
      vsync: this,
      duration: const Duration(milliseconds: 300),
    );
  }

  @override
  void didUpdateWidget(SynthesisPulseCell old) {
    super.didUpdateWidget(old);
    if (widget.isActive && !old.isActive) {
      _ensureController().forward(from: 0);
    }
  }

  @override
  void dispose() {
    _ctrl?.dispose();
    super.dispose();
  }

  @override
  Widget build(BuildContext context) {
    if (!widget.isActive || _ctrl == null) return widget.child;
    if (shouldReduceMotion(context)) return widget.child;
    return AnimatedBuilder(
      animation: _ctrl!,
      builder: (_, __) {
        final t = _ctrl!.value;
        final scale = 1.0 + 0.12 * Curves.easeOutBack.transform(
          t < 0.5 ? t * 2 : 2 - t * 2,
        );
        return Transform.scale(scale: scale, child: widget.child);
      },
    );
  }
}
```

- [ ] **Step 2: GameCellWidget'ta GelCellPainter'a isGlowing gec**

`game_cell_widget.dart` — GelCellPainter constructor'ina `isGlowing: data.isSynthesisResult` ekle.

- [ ] **Step 3: SynthesisPulseCell wrapper ekle**

SquashStretchCell'den sonra SynthesisPulseCell wrapper:
```dart
cellContent = SynthesisPulseCell(
  key: ValueKey(('synth_pulse', row, col)),
  isActive: data.isSynthesisResult,
  child: cellContent,
);
```

- [ ] **Step 4: Analyze + test**

Run: `flutter analyze lib/features/game_screen/ && flutter test test/features/game_screen_test.dart test/features/game_grid_builder_test.dart -v`
Expected: No issues, all tests PASS

- [ ] **Step 5: Commit**

```bash
git add lib/features/game_screen/effects/cell_effects.dart lib/features/game_screen/game_cell_widget.dart
git commit -m "feat(visual): synthesis pulse cell + glow integration"
```

- [ ] **Step 6: Code review — CD.6 tamamlandi**

Code reviewer agent ile review yap. Bulgulari `_dev/tasks/todo.md` backlog'a ekle.

---

## Task 6: CD.11 — Paket ekleme + interface genisletme

**Files:**
- Modify: `pubspec.yaml`
- Modify: `lib/services/notification_service.dart`

- [ ] **Step 1: Paketleri pubspec.yaml'a ekle**

```yaml
dependencies:
  firebase_messaging: ^15.1.0
  flutter_local_notifications: ^18.0.0
  timezone: ^0.9.4
```

Run: `flutter pub get`

- [ ] **Step 2: NotificationService interface'e initialize() ekle**

```dart
abstract class NotificationService {
  Future<void> initialize();  // YENi
  Future<bool> requestPermission();
  // ... mevcut metodlar
}
```

- [ ] **Step 3: StubNotificationService.initialize() no-op**

```dart
@override
Future<void> initialize() async {}
```

- [ ] **Step 4: Analyze**

Run: `flutter analyze lib/services/notification_service.dart`
Expected: No issues

- [ ] **Step 5: Commit**

```bash
git add pubspec.yaml pubspec.lock lib/services/notification_service.dart
git commit -m "feat(notification): add packages + initialize() to interface"
```

---

## Task 7: CD.11 — FirebaseNotificationService implementasyonu

**Files:**
- Modify: `lib/services/notification_service.dart`

- [ ] **Step 1: FirebaseNotificationService class olustur**

`notification_service.dart` dosyasina ekle:
```dart
class FirebaseNotificationService implements NotificationService {
  final FirebaseMessaging _fcm = FirebaseMessaging.instance;
  final FlutterLocalNotificationsPlugin _local = FlutterLocalNotificationsPlugin();
  StreamSubscription<String>? _tokenRefreshSub;

  @override
  Future<void> initialize() async {
    tz.initializeTimeZones();
    // Local notification init
    const androidSettings = AndroidInitializationSettings('@mipmap/ic_launcher');
    const iosSettings = DarwinInitializationSettings();
    await _local.initialize(const InitializationSettings(android: androidSettings, iOS: iosSettings));
    // FCM permission
    await _fcm.requestPermission(alert: true, badge: true, sound: true);
    // Token refresh listener
    _tokenRefreshSub = _fcm.onTokenRefresh.listen((_) {});
  }

  @override
  Future<bool> requestPermission() async {
    final settings = await _fcm.requestPermission();
    return settings.authorizationStatus == AuthorizationStatus.authorized;
  }

  @override
  Future<String?> getToken() async => _fcm.getToken();

  // scheduleStreakReminder, scheduleDailyPuzzleReminder, scheduleComebackNotification
  // cancelNotification, cancelAll — zonedSchedule ile implement et

  void dispose() { _tokenRefreshSub?.cancel(); }
}
```

- [ ] **Step 2: 3 senaryo scheduler metodlarini implement et**

`zonedSchedule` ile:
- Streak: `matchDateTimeComponents: DateTimeComponents.time` (her gun 20:00)
- Daily puzzle: `matchDateTimeComponents: DateTimeComponents.time` (her gun 10:00)
- Comeback: `scheduledDate: tz.TZDateTime.now(tz.local).add(Duration(days: 3))`

- [ ] **Step 3: Analyze**

Run: `flutter analyze lib/services/notification_service.dart`
Expected: No issues

- [ ] **Step 4: Commit**

```bash
git add lib/services/notification_service.dart
git commit -m "feat(notification): FirebaseNotificationService with 3 scenarios"
```

---

## Task 8: CD.11 — Platform yapilandirmasi

**Files:**
- Modify: `android/app/src/main/AndroidManifest.xml`
- Modify: `ios/Runner/Info.plist`
- Create: `ios/Runner/Runner.entitlements`

- [ ] **Step 1: Android izinleri**

`AndroidManifest.xml`'e `<uses-permission>` ekle:
```xml
<uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>
<uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED"/>
<uses-permission android:name="android.permission.WAKE_LOCK"/>
```

- [ ] **Step 2: iOS UIBackgroundModes**

`Info.plist`'e ekle:
```xml
<key>UIBackgroundModes</key>
<array>
  <string>remote-notification</string>
</array>
```

- [ ] **Step 3: iOS entitlements**

`ios/Runner/Runner.entitlements` olustur:
```xml
<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
  <key>aps-environment</key>
  <string>development</string>
</dict>
</plist>
```

- [ ] **Step 4: Commit**

```bash
git add android/app/src/main/AndroidManifest.xml ios/Runner/Info.plist ios/Runner/Runner.entitlements
git commit -m "feat(notification): Android + iOS platform permissions"
```

---

## Task 9: CD.11 — Provider, init, lifecycle + token sync

**Files:**
- Modify: `lib/providers/service_providers.dart`
- Modify: `lib/main.dart`
- Modify: `lib/data/remote/remote_repository.dart`
- Create: `supabase/migrations/20260323_device_tokens.sql`

- [ ] **Step 1: Provider ekle**

`service_providers.dart`:
```dart
final notificationServiceProvider = Provider<NotificationService>((ref) {
  if (kIsWeb) return StubNotificationService();
  return FirebaseNotificationService();
});
```

- [ ] **Step 2: main.dart init**

Firebase init blogundan sonra:
```dart
if (!kIsWeb) {
  final notifService = container.read(notificationServiceProvider);
  await notifService.initialize();
}
```

- [ ] **Step 3: Lifecycle observer**

`main.dart` — `WidgetsBindingObserver` ile foreground resume'da comeback iptal + yeniden zamanla.

- [ ] **Step 4: Token sync — RemoteRepository**

`remote_repository.dart`'a ekle:
```dart
Future<void> saveDeviceToken(String token, String platform) async {
  if (!isConfigured) return;
  try {
    await _client.from('device_tokens').upsert({
      'user_id': SupabaseConfig.currentUserId,
      'token': token,
      'platform': platform,
      'updated_at': DateTime.now().toIso8601String(),
    }, onConflict: 'user_id,platform');
  } catch (e) {
    if (kDebugMode) debugPrint('RemoteRepository: saveDeviceToken error: $e');
  }
}
```

- [ ] **Step 5: Migration SQL**

`supabase/migrations/20260323_device_tokens.sql`:
```sql
CREATE TABLE device_tokens (
  id uuid DEFAULT gen_random_uuid() PRIMARY KEY,
  user_id uuid REFERENCES auth.users(id) ON DELETE CASCADE,
  token text NOT NULL,
  platform text NOT NULL,
  updated_at timestamptz DEFAULT now()
);
ALTER TABLE device_tokens ENABLE ROW LEVEL SECURITY;
CREATE POLICY "Users manage own tokens" ON device_tokens
  FOR ALL USING (auth.uid() = user_id);
CREATE UNIQUE INDEX idx_device_tokens_user_platform
  ON device_tokens(user_id, platform);
```

- [ ] **Step 6: Analyze + build**

Run: `flutter analyze lib/providers/service_providers.dart lib/main.dart lib/data/remote/remote_repository.dart`
Expected: No issues

- [ ] **Step 7: Commit**

```bash
git add lib/providers/service_providers.dart lib/main.dart lib/data/remote/remote_repository.dart supabase/migrations/20260323_device_tokens.sql
git commit -m "feat(notification): provider, init, lifecycle observer, token sync"
```

---

## Task 10: CD.11 — Scheduler entegrasyonu + Settings toggle

**Files:**
- Modify: `lib/features/home_screen/home_screen.dart`
- Modify: `lib/features/game_screen/game_callbacks.dart`

- [ ] **Step 1: HomeScreen'de notification zamanlama**

`home_screen.dart` initState'te:
```dart
final notif = ref.read(notificationServiceProvider);
final l = ref.read(stringsProvider);
notif.scheduleStreakReminder(title: l.notifStreakTitle, body: l.notifStreakBody);
notif.scheduleDailyPuzzleReminder(title: l.notifDailyTitle, body: l.notifDailyBody);
```

- [ ] **Step 2: Game over'da comeback zamanlama**

`game_callbacks.dart` — `onGameOver` callback'te:
```dart
final notif = ref.read(notificationServiceProvider);
final l = ref.read(stringsProvider);
notif.scheduleComebackNotification(title: l.notifComebackTitle, body: l.notifComebackBody);
```

- [ ] **Step 3: L10n string'leri ekle**

`app_strings.dart` + 12 dilde override: `notifStreakTitle`, `notifStreakBody`, `notifDailyTitle`, `notifDailyBody`, `notifComebackTitle`, `notifComebackBody`.

- [ ] **Step 4: Analyze + test**

Run: `flutter analyze lib/features/home_screen/home_screen.dart lib/features/game_screen/game_callbacks.dart && flutter test test/ --exclude-tags=golden`
Expected: No new failures

- [ ] **Step 5: Commit**

```bash
git add lib/features/home_screen/home_screen.dart lib/features/game_screen/game_callbacks.dart lib/core/l10n/
git commit -m "feat(notification): scheduler integration + l10n strings"
```

- [ ] **Step 6: Code review — CD.11 tamamlandi**

Code reviewer agent ile review yap. Bulgulari `_dev/tasks/todo.md` backlog'a ekle.

---

## Task 11: Son dogrulama + todo guncelleme

- [ ] **Step 1: Tum testleri calistir**

Run: `flutter test test/ --exclude-tags=golden`
Expected: Mevcut basarisiz testlerde artis yok

- [ ] **Step 2: Build dogrula**

Run: `flutter build ios --simulator --dart-define-from-file=.env`
Expected: Build basarili

- [ ] **Step 3: `_dev/tasks/todo.md` guncelle**

CD.6, CD.10, CD.11 gorevlerini tamamlandi olarak isaretle. Code review bulgularini backlog'a ekle.

- [ ] **Step 4: Final commit**

```bash
git add _dev/tasks/todo.md
git commit -m "docs: update todo with CD.6/CD.10/CD.11 completion"
```
